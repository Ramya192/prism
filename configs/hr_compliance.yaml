# configs/hr_compliance.yaml
# Domain: HR — Compliance (stub)
# Not implemented yet. No `pipeline` block on purpose — ConfigLoader and
# AgentOrchestrator both treat status != "working" as "cannot be run",
# so this shows up in the domain list as "coming soon" without needing
# any special-casing elsewhere.

domain:
  id: hr_compliance
  name: "HR — Compliance"
  description: >
    Planned: policy-document RAG + compliance-gap detection over employee
    handbooks and HR policy documents. Same platform shape as
    bfsi_documents, different corpus and prompts.
  status: coming_soon
  input_type: document

classification_hints:
  keywords:
    - employee handbook
    - policy
    - PTO
    - leave
    - code of conduct
    - grievance
    - onboarding
